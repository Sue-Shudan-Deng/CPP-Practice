{"id":292967517,"lang":"python3","lang_name":"Python3","time":"3 years, 7 months","timestamp":1578668919,"status":15,"status_display":"Runtime Error","runtime":"N/A","url":"/submissions/detail/292967517/","is_pending":"Not Pending","title":"Decode String","memory":"N/A","code":"class Solution:\n    def decodeString(self, s: str) -> str:\n        stack = []\n        num = 0   # 当前的数字\n        curr = \"\"  # 当前正在处理的字符串\n        stack.append(curr)\n        \n        for char in s:\n            if char.isdigit():\n                num = 10 * num + int(char) # 因为数字本身不一定是个位数，所以不能直接取int \n            elif char == '[':\n                stack.append(str(num))  # 把当前的数字放进去\n                curr = \"\" # 清空当前正在处理的字符串，设置为开始处理\n                num = 0  # 因为数字暂时用不上了所以清0\n                \n            elif char == ']':\n                # 说明当前小范围的string已经处理完了，那么需要去整理下当前的string\n                cnt = int(stack.pop()) # 这里的stack的状态大致是: [\"\", '2', 'a', '3', 'bca', '4']\n                pre_str = stack.pop()\n                stack.append(pre_str + cnt * curr)   # 入栈，因为小范围处理完了还要处理大范围的\n                \n            else:\n                curr += char\n                \n        return stack[-1]","compare_result":"10000000000000000000000000000","title_slug":"decode-string","has_notes":false,"flag_type":1}